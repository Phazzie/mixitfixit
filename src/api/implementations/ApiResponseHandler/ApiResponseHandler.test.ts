import { ApiResponseHandler } from './ApiResponseHandler';

describe('ApiResponseHandler', () => {
  let instance: ApiResponseHandler;

  beforeEach(() => {
    instance = new ApiResponseHandler();
  });

  it('should be defined', () => {
    expect(instance).toBeDefined();
  });

  // Add more tests here
});
