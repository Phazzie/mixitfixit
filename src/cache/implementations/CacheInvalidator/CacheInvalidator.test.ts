import { CacheInvalidator } from './CacheInvalidator';

describe('CacheInvalidator', () => {
  let instance: CacheInvalidator;

  beforeEach(() => {
    instance = new CacheInvalidator();
  });

  it('should be defined', () => {
    expect(instance).toBeDefined();
  });

  // Add more tests here
});
