import { CacheStorage } from './CacheStorage';

describe('CacheStorage', () => {
  let instance: CacheStorage;

  beforeEach(() => {
    instance = new CacheStorage();
  });

  it('should be defined', () => {
    expect(instance).toBeDefined();
  });

  // Add more tests here
});
