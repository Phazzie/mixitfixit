import { ConfigLoader } from './ConfigLoader';

describe('ConfigLoader', () => {
  let instance: ConfigLoader;

  beforeEach(() => {
    instance = new ConfigLoader();
  });

  it('should be defined', () => {
    expect(instance).toBeDefined();
  });

  // Add more tests here
});
