import { PhaseStateManager } from './PhaseStateManager';

describe('PhaseStateManager', () => {
  let instance: PhaseStateManager;

  beforeEach(() => {
    instance = new PhaseStateManager();
  });

  it('should be defined', () => {
    expect(instance).toBeDefined();
  });

  // Add more tests here
});
