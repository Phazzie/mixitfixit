import { PhaseTransitioner } from './PhaseTransitioner';

describe('PhaseTransitioner', () => {
  let instance: PhaseTransitioner;

  beforeEach(() => {
    instance = new PhaseTransitioner();
  });

  it('should be defined', () => {
    expect(instance).toBeDefined();
  });

  // Add more tests here
});
