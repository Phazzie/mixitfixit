import { PhaseValidator } from './PhaseValidator';

describe('PhaseValidator', () => {
  let instance: PhaseValidator;

  beforeEach(() => {
    instance = new PhaseValidator();
  });

  it('should be defined', () => {
    expect(instance).toBeDefined();
  });

  // Add more tests here
});
