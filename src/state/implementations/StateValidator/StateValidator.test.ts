import { StateValidator } from './StateValidator';

describe('StateValidator', () => {
  let instance: StateValidator;

  beforeEach(() => {
    instance = new StateValidator();
  });

  it('should be defined', () => {
    expect(instance).toBeDefined();
  });

  // Add more tests here
});
