import { ValidationRuleEngine } from './ValidationRuleEngine';

describe('ValidationRuleEngine', () => {
  let instance: ValidationRuleEngine;

  beforeEach(() => {
    instance = new ValidationRuleEngine();
  });

  it('should be defined', () => {
    expect(instance).toBeDefined();
  });

  // Add more tests here
});
